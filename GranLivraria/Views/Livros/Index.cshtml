@model IEnumerable<GranLivraria.Models.Livro>

@{
    ViewData["Title"] = "Catálogo de Tomes";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="page-title">📚 Catálogo de Tomes</h1>
    <a asp-action="Create" class="btn btn-primary">
        <i class="fas fa-feather-alt me-2"></i>Novo Tomo
    </a>
</div>

@if (!Model.Any())
{
    <div class="book-card text-center py-5">
        <i class="fas fa-book-dead fa-4x mb-3" style="color: #8B4513;"></i>
        <h3 style="color: #8B0000;">Estante Vazia</h3>
        <p class="lead" style="color: #5D4037;">Nenhum tomo encontrado nos arquivos...</p>
        <a asp-action="Create" class="btn btn-primary mt-3">
            <i class="fas fa-feather-alt me-2"></i>Escrever o Primeiro Tomo
        </a>
    </div>
}
else
{
    <div class="table-responsive shadow-vintage">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>Título do Tomo</th>
                    <th>Gênero</th>
                    <th>Valor</th>
                    <th>Estoque</th>
                    <th>Ações</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            <strong style="color: #8B0000;">@Html.DisplayFor(modelItem => item.Titulo)</strong>
                        </td>
                        <td>
                            <span class="book-stock">@Html.DisplayFor(modelItem => item.Genero.Nome)</span>
                        </td>
                        <td class="book-price">@Html.DisplayFor(modelItem => item.Preco)</td>
                        <td>
                            <span class="badge" style="background: #8B0000; color: #F5E6CA; padding: 8px 12px;">
                                @Html.DisplayFor(modelItem => item.Estoque) unidades
                            </span>
                        </td>
                        <td>
                            <div class="btn-group" role="group">
                                <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-outline-primary">
                                    <i class="fas fa-scroll"></i>
                                </a>
                                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-primary">
                                    <i class="fas fa-edit"></i>
                                </a>
                                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-outline-danger">
                                    <i class="fas fa-trash-alt"></i>
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
}